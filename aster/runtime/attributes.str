module attributes

imports
  libstratego-lib

signature constructors
  EvalFailed : Term

strategies

  get-cached-attribute(|attribute-cache, node, name) =
    key := (node, name);
    !attribute-cache;
    hashtable-get(|key)
  
  set-cached-attribute(|attribute-cache, node, name, value) =
    where(
      key := (node, name);
      !attribute-cache;
      hashtable-put(|key, value)
    )

  attribute-cache =
    lookup-table(|"attribute-cache")
