module decorator-duplication

strategies
  
  main-decorator-duplication = id

attributes

  // Gives a warning
  //
  // See bad-decorator-duplication.astr

  decorator foo(a) = a

  decorator bar(a) = a

  def foo foo bar decorator-duplication = id
  
  def foo foo bar decorator-duplication = id
